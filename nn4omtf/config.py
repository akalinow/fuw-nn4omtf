
# Configuration saved/loaded to/from config.json in network directory
# Config can be tuned
# It also containing basic stats about epochs trained 

_opts = [
    'pt_bins',
    

    'epochs_trained',
    'train_dataset',
    'valid_dataset',
    
]

_cli_opts = [
    'train_dataset',
    'valid_dataset',

]
